{
  "name": "most",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./input.css -o ./dist/output.css --minify",
    "dev": "npx @tailwindcss/cli -i ./input.css -o ./dist/output.css --watch",
    "serve": "npx live-server --port=3000",
    "start": "concurrently \"npm run dev\" \"npm run serve\"",
    "format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "tailwindcss": "^4.2.2"
  }
}
